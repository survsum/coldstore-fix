// This file is kept for compatibility but NextAuth is not actively used.
// Admin authentication uses cookie-based auth (/api/admin/login).
// User authentication uses custom JWT (/api/auth/user/*).

export const authOptions = {};
